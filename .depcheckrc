# Ignore false-positives
ignores: [
    # Used in scripts/CI
    "rimraf", 
    "depcheck", 
    "improved-yarn-audit",
    "eslint-import-resolver-typescript",

    # Used in tests
    "ts-jest", 

    # Used in sources
    "pino-pretty", 
]
skip-missing: true