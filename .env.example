# Copy to .env and fill in your actual values.
# .env (with secrets) should be in .gitignore.

# --- Application ---
APP_NAME='tBTC Relayer (Dev)'
APP_VERSION='1.0.0-dev'
VERBOSE_APP=true
API_ONLY_MODE=true
ENABLE_CLEANUP_CRON=false
NODE_ENV='development' # Or 'production', 'test'

# --- Server Ports ---
HOST_PORT=4000
APP_PORT=3000

# --- CORS ---
CORS_ENABLED=true
CORS_URL='http://localhost:4000'

# --- Storage ---
# JSON_PATH is still in AppConfig, if used for other purposes.
JSON_PATH='./data/'

# --- Timers ---
CLEAN_QUEUED_TIME=48 # Hours
CLEAN_FINALIZED_TIME=12 # Hours
CLEAN_BRIDGED_TIME=12 # Hours

# --- Database ---
# DATABASE_URL is a required environment variable for AppConfigLoader.
# Example for local development (uses postgres user/db with password postgres):
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/tbtc_relayer?schema=public
# For Docker, this might be overridden in docker-compose.yml, e.g.:
# DATABASE_URL=postgresql://postgres:postgres@postgres:5432/tbtc_relayer?schema=public

# --- Chain-Specific Settings ---
# Add private keys and other sensitive chain-specific data here.
# The TypeScript config files (e.g., sepolia.chain.ts) are responsible for loading these.

CHAIN_SEPOLIATESTNET_PRIVATE_KEY='your_sepolia_testnet_private_key_here'
CHAIN_SOLANADEVNET_PRIVATE_KEY='CHAIN_SOLANADEVNET_PRIVATE_KEY'
CHAIN_STARKNETTESTNET_PRIVATE_KEY='CHAIN_STARKNETTESTNET_PRIVATE_KEY'
CHAIN_SUITESTNET_PRIVATE_KEY='CHAIN_SUITESTNET_PRIVATE_KEY'

# Add other chain-specific ENV variables as needed by your TS config files, following a similar pattern:
# CHAIN_<NORMALIZED_CHAIN_NAME>_<VARIABLE_NAME>=value
# e.g., CHAIN_MAINNET_API_KEY='your_mainnet_api_key'