# .semgrep.yml — managed by tlabs-xyz/devops sync:semgrep
# Regenerate: bun run sync:semgrep --generate --repo tlabs-xyz/tbtc-crosschain-relayer
#
# Registry rulesets: p/typescript p/secrets
# External sources: trailofbits (see semgrep/sources.yml)
# Run locally: semgrep --config .semgrep.yml --config p/typescript --config p/secrets --config r/trailofbits.javascript --error
rules: []
