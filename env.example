# Copy to .env and fill in your actual values.
# .env (with secrets) should be in .gitignore.


# =============================================================================
# --- APPLICATION ---
# =============================================================================

# --- Server ---
APP_NAME='tBTC Relayer (Dev)'
APP_VERSION='1.0.0-dev'
VERBOSE_APP=true
API_ONLY_MODE=true
ENABLE_CLEANUP_CRON=false
NODE_ENV='development' # Or 'production', 'test'

# --- Server Ports ---
HOST_PORT=4000
APP_PORT=3000

# --- CORS ---
CORS_ENABLED=true
CORS_URL='http://localhost:4000'

# --- Timers ---
CLEAN_QUEUED_TIME=48 # Hours
CLEAN_FINALIZED_TIME=12 # Hours
CLEAN_BRIDGED_TIME=12 # Hours

# --- Database ---
# PostgreSQL Connection Details
# These are used by the tbtc-relayer-dev service on docker-compose
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=tbtc_relayer

# Example for local development (uses postgres user/db with password postgres):
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/tbtc_relayer?schema=public
# For Docker, this might be overridden in docker-compose.yml, e.g.:
# DATABASE_URL=postgresql://postgres:postgres@postgres:5432/tbtc_relayer?schema=public

# =============================================================================
# --- CHAIN CONFIGURATION ---
# =============================================================================
# Most chain configuration (RPC URLs, contract addresses, start blocks) has been
# moved to ./config/chain/*.chain.ts files with smart defaults and fallbacks.
# Only sensitive values and deployment-specific overrides remain here.

# --- Supported Chains ---
# Comma-separated list of chain keys to activate.
# These keys must match the keys defined in the chainSchemaRegistry in config/index.ts
# Available chains: sepoliaTestnet, solanaDevnet, starknetTestnet, suiTestnet, 
#                   arbitrumMainnet, baseMainnet, baseSepoliaTestnet, solanaDevnetImported
# Example: SUPPORTED_CHAINS=sepoliaTestnet,solanaDevnet,arbitrumMainnet,baseMainnet
# If left empty or undefined, all configured chains will be attempted.
SUPPORTED_CHAINS=sepoliaTestnet,solanaDevnet,starknetTestnet,suiTestnet,arbitrumMainnet,baseMainnet,baseSepoliaTestnet,solanaDevnetImported

# =============================================================================
# --- CHAIN PRIVATE KEYS (SENSITIVE - REQUIRED) ---
# =============================================================================
# These are the only values that MUST be set in environment files
# All other chain configuration has defaults in ./config/chain/ files
#
# IMPORTANT: Different chain types use different private key formats:
# - EVM chains (Ethereum, Base, Arbitrum): Standard Ethereum private key (hex string, 64 chars + 0x prefix)
# - Solana: Base58 encoded private key or JSON array format (typically 87-88 characters)
# - Starknet: Hex private key for Starknet account (64 chars + 0x prefix)
# - Sui: Hex private key for Sui account (64 chars + 0x prefix)

# --- EVM Chain Private Keys ---
CHAIN_SEPOLIATESTNET_PRIVATE_KEY='your_sepolia_testnet_private_key_here'
CHAIN_ARBITRUMMAINNET_PRIVATE_KEY='your_arbitrum_mainnet_private_key_here'
CHAIN_BASEMAINNET_PRIVATE_KEY='your_base_mainnet_private_key_here'
CHAIN_BASESEPOLIATESTNET_PRIVATE_KEY='your_base_sepolia_private_key_here'

# --- Non-EVM Chain Private Keys ---
CHAIN_SOLANADEVNET_PRIVATE_KEY='your_solana_devnet_private_key_here'
CHAIN_STARKNETTESTNET_PRIVATE_KEY='your_starknet_testnet_private_key_here'
CHAIN_SUITESTNET_PRIVATE_KEY='your_sui_testnet_private_key_here'

# --- Legacy/Imported Configuration ---
# Special configuration for imported Solana devnet setup
CHAIN_SOLANADEVNETIMPORTED_SOLANA_PRIVATE_KEY='your_solana_devnet_imported_private_key_here'
# Optional configurations for Solana devnet imported (with defaults if not provided):
CHAIN_SOLANADEVNETIMPORTED_L2_RPC='https://api.devnet.solana.com'
CHAIN_SOLANADEVNETIMPORTED_L2_WS_RPC='wss://api.devnet.solana.com'
# CHAIN_SOLANADEVNETIMPORTED_L2_START_BLOCK=123456
# CHAIN_SOLANADEVNETIMPORTED_SOLANA_KEY_BASE='optional_key_base'

# =============================================================================
# --- OPTIONAL OVERRIDES ---
# =============================================================================
# The following variables are OPTIONAL. Chain configs provide smart defaults.
# Only set these if you need to override the defaults for specific deployments.

# --- Custom RPC URLs (Optional - overrides chain config defaults) ---
# Uncomment and set only if you need custom RPC endpoints

# Shared Ethereum L1 RPC URLs (used by all L2 chains)
ETHEREUM_MAINNET_RPC='https://ethereum-mainnet.rpc.grove.city/v1/62b3314e123e6f00424e5e75'
ETHEREUM_SEPOLIA_RPC='https://ethereum-sepolia.rpc.grove.city/v1/62b3314e123e6f00424e5e75'

# L2-specific RPC URLs (EVM Chains)
# CHAIN_ARBITRUMMAINNET_L2_RPC='https://custom-arbitrum-rpc.com' 
# CHAIN_ARBITRUMMAINNET_L2_WS_RPC='wss://custom-arbitrum-ws.com'
# CHAIN_BASEMAINNET_L2_RPC='https://custom-base-rpc.com'
# CHAIN_BASEMAINNET_L2_WS_RPC='wss://custom-base-ws.com'
# CHAIN_BASESEPOLIATESTNET_L2_RPC='https://custom-base-sepolia-rpc.com'
# CHAIN_BASESEPOLIATESTNET_L2_WS_RPC='wss://custom-base-sepolia-ws.com'

# --- Custom Start Blocks (Optional - overrides chain config defaults) ---
# Uncomment and set only if you need different start blocks for event scanning
# CHAIN_ARBITRUMMAINNET_L2_START_BLOCK=247865814
# CHAIN_BASEMAINNET_L2_START_BLOCK=26922966
# CHAIN_BASESEPOLIATESTNET_L2_START_BLOCK=123456

# --- Custom L1 Confirmations (Optional - overrides chain config defaults) ---
# Number of block confirmations required on L1 before processing transactions
# CHAIN_ARBITRUMMAINNET_L1_CONFIRMATIONS=6
# CHAIN_BASEMAINNET_L1_CONFIRMATIONS=6
# CHAIN_BASESEPOLIATESTNET_L1_CONFIRMATIONS=3
